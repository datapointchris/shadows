# API Reference

The user-facing contract of the `shadows` binary. Reach for this when you need a command's exact
flags or behavior rather than an explanation of the design.

## Documentation

- [Command Reference](COMMANDS.md) — Every subcommand with its flags, arguments and output, plus global flags, exit codes, environment variables and worked examples
